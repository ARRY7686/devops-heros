apiVersion: apps/v1
kind: Deployment
metadata:
  name: selector-error-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: correct-app-name
  template:
    metadata:
      labels:
        # FIXED: Label matches selector.matchLabels!
        app: correct-app-name
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
